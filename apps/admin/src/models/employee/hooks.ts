import { EmployeeSchema } from './document';

export function attachHooks() {
  // Currently no pre-save hooks needed for Employee model
  // This file is kept for consistency with the model pattern
  // and for future hook additions if needed
}
