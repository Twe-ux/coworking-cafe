import { TurnoverSchema } from "./document";

export function attachHooks() {
  // Add pre-save or pre-update hooks here if needed in the future

  // Example:
  // TurnoverSchema.pre("save", async function (next) {
  //   // Custom logic before saving
  //   next();
  // });
}
