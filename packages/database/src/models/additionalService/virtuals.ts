import {
  AdditionalServiceDocument,
  AdditionalServiceSchema,
} from "./document";

/** Virtual object derived from the {@link AdditionalServiceSchema}. */
export interface VirtualAdditionalService extends AdditionalServiceDocument {
  // No virtuals defined for AdditionalService yet
}
