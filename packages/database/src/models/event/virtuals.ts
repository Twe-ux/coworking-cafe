import { EventDocument } from "./document";

/**
 * Virtual properties for Event model
 */
export interface VirtualEvent extends EventDocument {
  // Add custom virtual properties here if needed in the future
  // Example: fullDateTime: string;
}
