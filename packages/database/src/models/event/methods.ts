import { EventDocument } from "./document";

/**
 * Instance methods for Event model
 */
export interface EventMethods {
  // Add custom instance methods here if needed in the future
  // Example: isUpcoming(): boolean;
}
