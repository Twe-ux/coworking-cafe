import { SpaceDocument, SpaceSchema } from "./document";

/** Virtual object derived from the {@link SpaceSchema}. */
export interface VirtualSpace extends SpaceDocument {
  // No virtuals defined for Space yet
}
